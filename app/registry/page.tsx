import { RegistryPage } from '@/components/pages';

import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Forever Fest 2026 - Registry',
};

const Registry: React.FC = () => <RegistryPage />;
export default Registry;
